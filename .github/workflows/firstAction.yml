name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-lastest
    steps:
      - name: Print greetings
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done bye"
